import { createTheme } from "@mui/material/styles";

let theme = createTheme({
  palette: {
    gray: "#AEA79F",
    white: "#fff",
    black: "#000",
    primary: {
      main: "#9E82EB",
      right: "#EB829B",
      left: "#CFEB82",
      last: "#82EBD2",
    },
    secondary: {
      main: "#edf2ff",
    },
  },
});
export default theme;
